import 'package:flutter/material.dart';
import 'package:flutter_demo/Screeens/Signup/body.dart';

class SignUpScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.black,
      body: Body(
        child: Column(),
      ),
    );
  }
}
